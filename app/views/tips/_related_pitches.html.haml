#related_pitches
  - if related_pitches.any?
    - related_pitches.each do |pitch|
      .related_pitch
        = link_to pitch.headline, pitch_url(pitch)
  - else
    %p No hay artículos relacionados. Revisalos de vez en cuando por si hubiera alguno. 
