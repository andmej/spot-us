- unless @story.errors.empty?
  - content_for :error do
    = error_messages_for :story

- @title = "Spot.us - Edita tu historia"
.block-spacer
%h3 Edita tu historia

-# first column content 
.span-715.box_white
  = render :partial => 'form'

-# second column content
= render :partial => 'sidebar'
.clear
.block-spacer
