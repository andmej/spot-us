.block-spacer
%h3== Hacer un comentario en #{@pitch.headline}
.span-715.box_white
  = render :partial => 'news_items/user', :locals => { :news_item => @pitch }
  - form_for([@pitch, @post]) do |f|
    = render :partial => 'form', :locals => {:f => f}
